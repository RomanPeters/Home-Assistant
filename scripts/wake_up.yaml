alias: Wake_up
sequence:
  - alias: Wake up light
    service: homeassistant.turn_on
    data:
      entity_id: group.lichten
      color_name: white
      transition: 1800
      brightness: 255
  - delay:
      minutes: 60
#  - alias: Set alarm
#      service: input_select.select_option
#      data:
#        entity_id: input_select.alarm_time
#        # not the right day, doesn't work, subtract 60 min
#        option: "{{ states.calendar.betaplanner.attributes.start_time[10:16] }}"  # must be rounded to %5
  - alias: Lights off
    service: homeassistant.turn_off
    data:
      entity_id: group.lichten



  

#  - alias: Start sport

#  - alias: Start ontbijt ttp
