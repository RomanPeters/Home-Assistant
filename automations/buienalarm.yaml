- id: buienalarm
  alias: Buienalarm Notification

  trigger:
    platform: state
    entity_id: sensor.br_precipitation_forecast_total
    from: '0.0'

  action:
    service: notify.notify
    data:
      message: "Precipitation expected."

