- id: boot
  alias: At start
  trigger:
    - platform: homeassistant
      event: start
  action:
    - service: switch.turn_on
      entity_id: switch.pihole
    - service: switch.turn_on
      entity_id: switch.zigbee2mqtt_pairing

