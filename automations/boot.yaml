# Run the boot script
- id: boot
  alias: Boot

  trigger:
    - platform: homeassistant
      event: start

  action:
    - service: script.boot

    - service: notify.notify
      data:
        message: "Home Assistant is online"
