
- id: boot
  alias: At start
  trigger:
    - platform: homeassistant
      event: start
  action:
    - service: group.turn_on
      entity_id: group.all_automations
      #    - service: shell_command.install_deps
